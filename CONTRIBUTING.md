# 贡献与代码质量规范

本项目首先是一个用于深入学习 AIOps、DevOps、SRE 和软件架构的作品。代码是否容易解释、测试和修改，与功能数量同等重要。

## 开始修改前

1. 先阅读对应阶段 README，确认数据流和当前限制。
2. 确认修改属于领域、应用、端口、适配器还是 API，不跨层走捷径。
3. 新增外部依赖前说明现有标准库或组件为什么不能满足需求，以及对 4GB 环境的影响。
4. 涉及协议、权限、存储或核心架构取舍时增加 ADR。

## 代码组织

- 一个文件只承担一个明确职责，禁止创建不断扩张的 `utils` 或 `helpers`。
- 领域层不能导入 FastAPI、psycopg、gRPC、Docker 或具体模型客户端。
- API Router 只做边界校验和用例调用，不实现算法、SQL 或系统命令。
- 函数圈复杂度目标不超过 10，嵌套原则上不超过 3 层。
- 手写源文件超过 300 行会被文档检查拒绝；应优先按业务职责拆分。
- 错误必须保留上下文并转换为领域或接口错误，禁止空捕获和无限重试。

## 中文注释

- 每个手写源文件必须在顶部说明职责。
- 公开类、函数、接口、模型和配置必须有中文说明。
- 异常检测、RCA、RAG、审批、哈希、幂等、插件升级、权限和回滚等关键代码需要逐行说明数据和安全原因。
- 注释回答“为什么”和“失败会怎样”，不能只翻译语法。
- 修改实现时必须同步修改注释；过期注释按代码缺陷处理。

## 测试与验证

提交前至少执行：

```bash
cd control-plane
../.venv/bin/python -m pytest
../.venv/bin/python -m ruff check src tests
../.venv/bin/python -m mypy src

cd ../agent
go test -race ./...
go vet ./...

cd ../web
pnpm run build

cd ..
.venv/bin/python scripts/check_chinese_docs.py
docker compose -f deployments/compose.yaml config --quiet
```

算法修改必须使用固定数据集重新生成评测结果，并解释 Precision、Recall、FPR 或 Top-K RCA 的变化。不得只展示最优样例。

## 阶段文档

新增或修改阶段能力时，同步更新对应阶段 README，至少包含：

- 组件和接口变化。
- 端到端数据流与 Mermaid 图。
- 技术选型和被放弃方案。
- 核心算法、输入输出、复杂度和局限。
- 实际遇到的错误、排查证据和修复方式。
- 测试结果、资源占用、安全边界和下一阶段事项。

## 代码评审清单

- [ ] 文件职责单一，依赖方向正确。
- [ ] 没有任意 Shell、越权目标或未限制重试。
- [ ] 关键状态变化在副作用前持久化。
- [ ] 重复请求和并发请求不会重复改变系统。
- [ ] 模型输出被视为不可信数据，并经过工具、计划和审批边界。
- [ ] 新插件具有版本、权限、SHA-256、健康检查和失败回滚。
- [ ] 新代码具有中文说明和正常、异常、边界测试。
- [ ] 阶段 README、根 README 和实际状态一致。
