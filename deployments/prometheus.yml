# Prometheus 首版只采集 Agent；后续插件通过标准服务发现追加目标。
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aiops-agent
    static_configs:
      - targets: ["agent:9105"]
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:9464"]
